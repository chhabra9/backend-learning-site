const router = require('express').Router();
const {createModuleController} = require('../controller/module.controller');
const {verifyTokenAndInstructor} =require('../services/verifyToken');
router.post('/',)
module.exports =router